{
  "name": "gabys-hair-studio",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix server && npm install --prefix web && npm run build --prefix web && npm run build --prefix server",
    "start": "npm run start --prefix server",
    "dev:server": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix web",
    "seed": "npm run seed --prefix server"
  }
}
